name: dragons
channels:
  - astroconda
dependencies:
  - astropy
  - dateutil
  - pip
  - pip:
    - future
    - coveralls
    - "--editable=git+git://github.com/GeminiDRSoftware/DRAGONS@master#egg=DRAGONS"
